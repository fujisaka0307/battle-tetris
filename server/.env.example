# サーバーポート
PORT=4000

# Azure AD / Entra ID — テナントID（JWT検証に使用）
AZURE_TENANT_ID=xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx

# Azure AD / Entra ID — クライアントID（JWT audience 検証に使用）
AZURE_CLIENT_ID=xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx

# IDトークンから Enterprise ID を読み取るクレーム名
ENTERPRISE_ID_CLAIM=preferred_username

# 認証をバイパス（ローカル開発用）
# SKIP_AUTH=true

# AWS Bedrock — 全レベルでLLM使用（未設定時はヒューリスティックにフォールバック）
# AWS SDK のデフォルトクレデンシャルチェーン（環境変数、~/.aws/credentials、SSO等）を使用
# AWS_REGION=ap-northeast-1
#
# レベル別モデル（環境変数で上書き可能）
# Lv1-3: Haiku, Lv4-7: Sonnet, Lv8-10: Claude (Opus)
# BEDROCK_MODEL_HAIKU=us.anthropic.claude-3-5-haiku-20241022-v1:0
# BEDROCK_MODEL_SONNET=us.anthropic.claude-3-5-sonnet-20241022-v2:0
# BEDROCK_MODEL_CLAUDE=us.anthropic.claude-3-opus-20240229-v1:0
